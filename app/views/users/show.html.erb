<h1><%= @user.name %>のページ</h1>
<p>プロフィール画像<%= image_tag @user.image.url %></p>
<p>メールアドレス: <%= @user.email %></p>
 <%= link_to 'お気に入り一覧', "/users/#{@user.id}/favorites" %>
 <%= link_to 'プロフィール編集', edit_user_path(@user.id) %></td>
